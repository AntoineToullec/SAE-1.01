programme SUDOKU c'est
  const int VALSUD=45*9;

début
tant que ((SUDOKU!=VALSUD) && (TAB n'a plus de 0)) faire
   fonct affichergrille
   écrireEcran(Voulez-vous ajouter ou suprrimer un élément? A/D);
   lireClavier(%c,choix);
   fonct afficherErreurCH   
   écrireEcran(Quelle est la colonne et la ligne ? (ex: a2));
   lireClavier(%c,coord);
   font afficheErreurCO
   si
   écrireEcran(Quel est le chiffre eniter de 1 à 9);
   lireClavier(%d, chiffre);
   fonct afficheErreurCHIF


fin

